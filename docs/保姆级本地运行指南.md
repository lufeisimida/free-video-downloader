# 🚀 保姆级本地运行指南

本文档手把手教你在本地跑起来 AI 万能视频下载总结器的完整前后端，**零基础也能跟着做**。



## 前置要求

| 工具 | 版本要求 | 检查命令 | 安装方式 |
|------|----------|----------|----------|
| Python | ≥ 3.10 | `python3 --version` | [官网下载](https://www.python.org/downloads/) |
| Node.js | ≥ 18 | `node -v` | [官网下载](https://nodejs.org/en)（推荐 LTS 版本） |
| npm | ≥ 9 | `npm -v` | 随 Node.js 一起安装 |
| ffmpeg | 任意 | `ffmpeg -version` | [官网下载](https://ffmpeg.org/download.html) 或 `brew install ffmpeg`（macOS） |
| Git | 任意 | `git --version` | [官网下载](https://git-scm.com/) |

💡 关于 ffmpeg：ffmpeg 用于合并视频和音频流（如 YouTube 高清视频的画面和声音是分开的）。如果不安装，项目仍可运行，但下载高清视频时会降级为低画质单一格式。**强烈建议安装。**



## 第一步：克隆项目

```bash
git clone https://github.com/liyupi/free-video-downloader.git
cd free-video-downloader
```

💡 如果 GitHub 访问较慢，可以在 Gitee 上导入仓库后克隆。



## 第二步：获取 API Key

项目的 AI 总结功能至少需要选择 **1 个大模型 API Key**，另外 1 组支付配置为可选。



### 必需：AI 大模型连接（Base URL + API Key + 模型名）

AI 大模型用于视频总结功能（字幕提取后调用大模型生成总结、思维导图、问答）。系统统一使用 **OpenAI 兼容接口**，适合官方 API 和各类中转站。

你只需要准备三样东西：
1. **Base URL**：例如 `https://api.openai.com/v1` 或中转站提供的地址（通常带 `/v1`）
2. **API Key**：中转站或官方平台发放的密钥（一般以 `sk-` 开头）
3. **模型名称**：例如 `gpt-4o-mini`、`deepseek-chat`、`claude-3-5-sonnet` 等（以你中转站实际支持的模型名为准）

配置方式二选一：
- 在 `backend/.env` 里填写 `LLM_BASE_URL` / `LLM_API_KEY` / `LLM_MODEL`
- 或启动后在前端顶部「模型配置」面板中填写并保存（页面配置优先于 `.env`）

💡 多数中转站都兼容 OpenAI 的 `/v1/chat/completions`，直接填中转地址即可。

![](https://pic.yupi.icu/1/1772534666069-27ae688b-d917-4341-ae42-a3dbb5b26b89.png)



### 可选：Stripe 支付配置

Stripe 用于 VIP 会员的在线支付功能。**本地学习时可以不配置**，不影响视频下载和 AI 总结等核心功能。

如果你想体验完整的支付流程：

1）访问 [Stripe Dashboard](https://dashboard.stripe.com/)，注册并登录

2）确保处于「测试模式」（Test mode）

![](https://pic.yupi.icu/1/1773828514541-4e997b12-9f0e-4e4a-8fd8-579a3ca0f3fa-20260323193408848.png)

3）在「开发者 → API 密钥」获取 Secret Key（以 `sk_test_` 开头）

4）在「产品」页面创建一个产品和价格，获取 Price ID（以 `price_` 开头）

![](https://pic.yupi.icu/1/1773833187544-ca540fca-38ca-4225-a7aa-f9f502e0dcfc.png)

5）安装 [Stripe CLI](https://docs.stripe.com/stripe-cli)，先执行 `stripe login` 登录：

![](https://pic.yupi.icu/1/1773829060696-1563dc00-3483-498c-a229-bf7c87e1308d.png)

然后运行以下命令启动本地 Webhook 转发：

```bash
stripe listen --forward-to localhost:8000/api/payment/webhook
```
终端会输出 Webhook Signing Secret（以 `whsec_` 开头）

![](https://pic.yupi.icu/1/1773831388188-6f926d9a-c9c6-4a38-a5d4-8f56fac72fce-20260323193106527.png)



## 第三步：配置环境变量

```bash
cd backend
cp .env.example .env
```

用任意文本编辑器打开 `backend/.env` 文件，填入你的配置：

```env
# ✅ 必填 - OpenAI 兼容模型连接（官方 API 或中转站）
LLM_BASE_URL=https://你的中转站地址/v1
LLM_API_KEY=sk-你的真实key
LLM_MODEL=gpt-4o-mini

# 无字幕视频语音转写（首次使用会自动下载模型）
WHISPER_MODEL=small
WHISPER_DEVICE=cpu
WHISPER_COMPUTE_TYPE=int8
WHISPER_LANGUAGE=
WHISPER_BEAM_SIZE=5
# Hugging Face 连接不稳定时可填写模型镜像地址
WHISPER_HF_ENDPOINT=

# 开发/自用开关
DISABLE_SUMMARY_PAYWALL=false
SUPERUSER_EMAILS=
PAYMENT_ENABLED=true

# ✅ 必填 - JWT 密钥，用于用户登录认证
# 本地开发可以随便写一个字符串，生产环境请使用强随机值
JWT_SECRET=my-local-dev-secret-123

# 🔧 选填 - Stripe 支付配置（不配置不影响核心功能）
STRIPE_SECRET_KEY=sk_test_你的stripe密钥
STRIPE_WEBHOOK_SECRET=whsec_你的webhook密钥
STRIPE_PRICE_ID_MONTHLY=price_你的价格id

# 🔧 选填 - 前端 URL，Stripe 支付完成后跳转地址
FRONTEND_URL=http://localhost:5173
```

| 字段 | 必填 | 用途 |
|------|------|------|
| `LLM_BASE_URL` | ✅ 必填 | OpenAI 兼容接口地址，例如 `https://api.openai.com/v1` 或中转站 URL |
| `LLM_API_KEY` | ✅ 必填 | API 密钥；也可在前端「模型配置」中填写 |
| `LLM_MODEL` | ✅ 必填 | 模型名称，以中转站/官方实际支持的名称为准 |
| `WHISPER_MODEL` | 选填 | 无字幕视频的本地转写模型，默认 `small` |
| `WHISPER_DEVICE` / `WHISPER_COMPUTE_TYPE` | 选填 | CPU 推荐 `cpu` / `int8`；NVIDIA CUDA 推荐 `cuda` / `float16` |
| `WHISPER_LANGUAGE` | 选填 | 留空自动识别，也可以指定 `zh`、`en` 等 |
| `WHISPER_HF_ENDPOINT` | 选填 | Hugging Face 连接不稳定时填写可信的模型镜像地址 |
| `DISABLE_SUMMARY_PAYWALL` | 选填 | 设为 `true` 后关闭 AI 总结登录和次数限制，适合本地自用 |
| `SUPERUSER_EMAILS` | 选填 | 超级用户邮箱列表，多个邮箱用英文逗号分隔；这些账号登录后显示为 VIP 且 AI 总结无限次 |
| `PAYMENT_ENABLED` | 选填 | 设为 `false` 后后端支付接口关闭 |
| `JWT_SECRET` | ✅ 必填 | 用户登录 Token 的签名密钥，本地开发可随意设置 |
| `STRIPE_SECRET_KEY` | 🔧 选填 | Stripe 支付 API 密钥，不填则支付功能不可用 |
| `STRIPE_WEBHOOK_SECRET` | 🔧 选填 | Stripe Webhook 签名验证密钥 |
| `STRIPE_PRICE_ID_MONTHLY` | 🔧 选填 | Stripe 月度会员的价格 ID |
| `FRONTEND_URL` | 🔧 选填 | 支付完成后跳转地址，默认 `http://localhost:5173` |

⚠️ **注意**：`.env` 文件包含敏感信息，已被 `.gitignore` 排除，不会提交到代码仓库。



## 第四步：安装依赖

### 后端依赖（Python）

```bash
cd backend
pip install -r requirements.txt
```

💡 如果下载慢，可以切换为国内镜像：

```bash
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```



### 前端依赖（Node.js）

```bash
cd ../frontend
npm install
```

💡 如果 npm install 速度慢，可以先切换为国内镜像：
```bash
npm config set registry https://registry.npmmirror.com
```



## 第五步：初始化数据库

本项目使用 **SQLite**，**无需手动安装任何数据库软件**。

后端服务启动时会自动完成以下操作：
1. 在 `backend/data/` 目录下创建 `app.db` 数据库文件
2. 自动建表（`users` 用户表、`orders` 订单表）

💡 如果需要重置数据库，直接删除 `backend/data/app.db` 文件，重启后端即可自动重建。



## 第六步：启动项目

需要同时启动后端和前端，**打开两个终端窗口**：

**终端 1 启动后端：**

```bash
cd backend
python main.py
```

看到以下输出表示后端启动成功：

```
INFO:     Started server process [12345]
INFO:     Waiting for application startup.
INFO:     Application startup complete.
INFO:     Uvicorn running on http://0.0.0.0:8000 (Press CTRL+C to quit)
```

**终端 2 启动前端：**

```bash
cd frontend
npm run dev
```

看到以下输出表示前端启动成功：

```
VITE v7.x.x ready in xxx ms

➜  Local:   http://localhost:5173/
```



## 第七步：访问项目

打开浏览器，访问 http://localhost:5173 ，你将看到 AI 万能视频下载总结器的界面。



### 快速体验流程

1. 注册一个账号并登录
2. 在搜索框粘贴一个视频链接（如 B 站、YouTube、抖音），点击解析
3. 系统自动解析出视频标题、封面、可选格式，选择画质后下载
4. 解析视频后查看 AI 自动生成的总结和思维导图
5. 在 AI 总结页面输入问题，体验基于视频内容的智能问答

💡 AI 总结功能需要登录，免费用户每天可使用 3 次。



![解析下载视频并用 AI 总结](https://pic.yupi.icu/1/AI%25E8%25A7%2586%25E9%25A2%2591%25E4%25B8%258B%25E8%25BD%25BD+%25E6%2580%25BB%25E7%25BB%2593%25E5%258A%259F%25E8%2583%25BD.png)



### 后端 API 文档

后端基于 FastAPI，内置交互式 API 文档：

- Swagger UI：http://localhost:8000/docs
- ReDoc：http://localhost:8000/redoc



## 常见问题

### Q1：`ffmpeg` 未安装，下载视频报错或画质很低？

**原因**：高清视频的画面和声音是分开的，需要 ffmpeg 合并。

**解决**：
```bash
# macOS
brew install ffmpeg

# Ubuntu / Debian
sudo apt install ffmpeg

# Windows：从 https://ffmpeg.org/download.html 下载，解压后将 bin 目录加入 PATH
```

### Q2：`yt-dlp` 版本过低，解析视频失败？

**现象**：部分视频解析失败，报错 `Unable to extract` 或 `Unsupported URL`。

**解决**：
```bash
pip install --upgrade yt-dlp
```

### Q3：AI 总结报未配置模型 API Key？

**原因**：`.env` 文件未创建，或 `LLM_API_KEY` / `LLM_BASE_URL` / `LLM_MODEL` 未填写。

**解决**：确认 `backend/.env` 文件存在并填好三项连接信息；也可以在前端「模型配置」面板中填写后保存，再点「测试连接」验证。

### Q4：端口 8000 或 5173 被占用怎么办？

**解决**：
```bash
# 查看占用端口的进程
lsof -i :8000    # macOS / Linux

# 终止占用的进程
kill -9 <PID>
```

### Q5：前端请求后端接口返回 CORS 错误？

**解决**：确保通过 `http://localhost:5173` 访问前端页面。前端会自动将 `/api` 请求通过 Vite 代理转发到后端，本项目后端也已配置了 `allow_origins=["*"]`。

### Q6：下载抖音视频失败？

**解决**：
1. 确保使用的是抖音分享链接（如 `https://v.douyin.com/xxx`）
2. 检查网络是否正常，抖音接口可能有频率限制
3. 尝试升级 yt-dlp：`pip install --upgrade yt-dlp`

### Q7：Stripe 支付功能测试不成功？

**解决**：
1. 确保 `.env` 中三个 Stripe 配置项都已填写
2. 确保 Stripe Dashboard 处于「测试模式」
3. 测试支付时使用 Stripe 提供的测试卡号：`4242 4242 4242 4242`
4. 本地测试 Webhook 需运行 Stripe CLI：
   ```bash
   stripe listen --forward-to localhost:8000/api/payment/webhook
   ```

### Q8：`pip install` 安装依赖报错？

**常见情况**：
- **bcrypt 编译失败**：安装 C 编译工具。macOS 运行 `xcode-select --install`
- **Python 版本过低**：需要 Python ≥ 3.10
- **网络超时**：使用国内镜像 `-i https://pypi.tuna.tsinghua.edu.cn/simple`

### Q9：Windows 系统运行终端命令报错？

**解决**：建议在 VSCode 中把默认终端改为 Git Bash，或使用 PowerShell。避免使用 CMD。

### Q10：无字幕视频第一次总结很慢？

**原因**：第一次语音转写需要下载 `WHISPER_MODEL` 对应的模型，并在本机完成语音识别。默认 `small` 模型会缓存到 `backend/data/whisper-models`，转写文本会缓存到 `backend/data/transcripts`。

**建议**：纯 CPU 保持 `WHISPER_DEVICE=cpu` 和 `WHISPER_COMPUTE_TYPE=int8`；配置好 NVIDIA CUDA 后可使用 `WHISPER_DEVICE=cuda` 和 `WHISPER_COMPUTE_TYPE=float16`。



## 停止项目

在各终端窗口按 `Ctrl + C` 即可停止前后端服务。

数据库文件（`backend/data/app.db`）会保留，下次启动时数据不会丢失。



## 端口汇总

| 服务 | 默认端口 | 地址 | 说明 |
|------|----------|------|------|
| 后端 API | 8000 | http://localhost:8000 | FastAPI 服务 |
| 后端 API 文档 | 8000 | http://localhost:8000/docs | Swagger UI |
| 前端页面 | 5173 | http://localhost:5173 | Vite 开发服务器 |

## 开发时关闭付费功能

如果只是本地自用，不想登录或开通 VIP，后端设置：

```env
DISABLE_SUMMARY_PAYWALL=true
PAYMENT_ENABLED=false
```

前端设置 `frontend/.env`：

```env
VITE_PAYMENT_ENABLED=false
```

这样 AI 总结不再校验登录和每日次数，页面也会隐藏套餐价格和开通 VIP 入口。
